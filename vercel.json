{
  "framework": null,
  "buildCommand": "pnpm install && pnpm --filter @universal-fhevm/core build && pnpm --filter @universal-fhevm/react build && pnpm --filter react-vite build",
  "outputDirectory": "examples/react-vite/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    {
      "src": "/assets/(.*)",
      "dest": "/assets/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
